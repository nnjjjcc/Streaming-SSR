import { networkInterfaces } from 'os';
import { IPFamily } from './enum';
const ips = Object
    .values(networkInterfaces())
    .flat(1)
    .filter((v) => v !== undefined && !v.internal)
    .reduce((a, c) => a.set(c.family, c.address), new Map());
export function getIP(family) {
    return ips.get(family);
}
export function getIPv4() {
    return getIP(IPFamily.IPv4);
}
export function getIPv6() {
    return getIP(IPFamily.IPv6);
}
