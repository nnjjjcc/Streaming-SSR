/// <reference types="node" />
import { EventEmitter } from 'events';
import { ConnectionOptions } from '../interface';
export declare class Connection extends EventEmitter {
    private socket;
    private proto;
    private commandQueue;
    private connecting;
    private idle;
    constructor({ ip, port, password, connectionTimeout, tls, }: ConnectionOptions);
    private coordinate;
    command<T>(...parameters: (string | number)[]): Promise<T>;
    close(message: string, e?: Error): void;
}
