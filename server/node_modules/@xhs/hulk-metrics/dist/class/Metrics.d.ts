import { SummaryConfiguration } from 'prom-client';
import { ArtifactLabel, Observer } from '../interface';
export declare class Metrics {
    static pkgInfo: {
        name: string;
        version: string;
    } | undefined;
    static artifactLabels: Record<ArtifactLabel, string>;
    static defaultMetrics: void;
    static createObserver<T extends string>(configuration: SummaryConfiguration<ArtifactLabel | T>): Observer<T>;
}
