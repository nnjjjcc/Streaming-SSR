import { HulkState, HulkContext } from '../di';
import Koa = require('koa');
import Router = require('find-my-way');
export declare class RouterService {
    router: Router.Instance<Router.HTTPVersion.V1>;
    constructor(config?: Router.Config<Router.HTTPVersion.V1>);
    on<T = {}>(method: Router.HTTPMethod | Router.HTTPMethod[], path: string, options: Router.RouteOptions, handler: Koa.Middleware, afterRespond: ((ctx: HulkContext<HulkState & T>) => void) | undefined, store: Record<string, any> | null): void;
    lookup(beforeRouting?: (ctx: Koa.Context) => void): Koa.Middleware;
    prettyPrint(): string;
}
