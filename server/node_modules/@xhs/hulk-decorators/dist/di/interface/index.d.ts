import Koa = require('koa');
export interface HulkState {
    method: string;
    path: string;
    store: Record<string, any>;
    params: Record<string, string | undefined>;
    query?: Record<string, string | undefined>;
    body?: Record<string, any>;
    status: number;
    code: number;
    success: boolean;
    startTime: number;
    endTime: number;
    responseTime: number;
    /** only appears in afterRespond */
    sendTime: number;
}
export type HulkContext<STATE = {}, BODY = unknown, CONTEXT = Koa.DefaultContext> = Koa.ParameterizedContext<HulkState & STATE, CONTEXT, BODY>;
/** @ignore */
export type CustomDecorator = PropertyDecorator & ParameterDecorator & MethodDecorator & ClassDecorator;
